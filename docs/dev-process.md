# Development Process — Dental Clinic Staff App

> **Purpose:** This file is the single source of truth for the ordered development sequence of the Dental Clinic Staff/Admin Flutter Web App. It is a living document — update status and notes as milestones are completed.
>
> **Stack:** Flutter Web · Firebase Auth · Cloud Firestore · Cloud Functions (TypeScript) · Riverpod · go_router
>
> **Key references:**
> - Architecture: [dental-clinic-appointment-system-plan.md](./dental-clinic-appointment-system-plan.md)
> - Functions: [functions-api-contract.md](./functions-api-contract.md)
> - Decisions: [decisions-log.md](./decisions-log.md)

---

## Progress Legend

| Symbol | Meaning |
|---|---|
| ✅ | Completed & verified |
| 🔄 | In progress |
| ⏳ | Ready to start |
| 🔒 | Blocked — dependency must resolve first |

---

## Phase 1 — Backend Foundation ✅

> Goal: Build and test the core write-path Cloud Function with full transaction safety, security rules, and schema alignment.

### Step 1.1 — Schema & Types ✅
- Defined [schema-types.ts](../functions/src/schema-types.ts) as the single TypeScript source of truth for all Firestore document shapes.
- Typed all collections: `StaffUser`, `Service`, `ClinicSettings`, `Appointment`.
- Added additive fields (`bookingSource`, `createdBy`, `paid`, `reminderSent`).
- Corrected `AnalysisResults` to match patient app's actual array format: `AnalysisTag[]`.

### Step 1.2 — `createWalkInAppointment` Cloud Function ✅
- Implemented [createWalkInAppointment.ts](../functions/src/createWalkInAppointment.ts) with:
  - **Auth guard** — caller must have `users/{uid}` with `role in ['staff', 'admin']`.
  - **Input validation** — all required fields, ISO 8601 date parsing.
  - **Service lookup** — verifies service exists and is active.
  - **Slot snapping** — reads `clinicSettings/main.slotDurationMinutes` (default 30) and snaps `appointmentDateTime` to the nearest slot boundary to align with the patient app's discrete slot grid.
  - **Firestore transaction** — slot-conflict check + document write are atomic.
  - Walk-in document layout: `userId/userEmail` null, `bookingSource: "staff_walkin"`, `reason` set to service name for backward compatibility.
- Exported via [index.ts](../functions/src/index.ts).
- Verified: **46/46 Jest tests pass** (including 3 concurrency double-booking prevention tests and 6 slot-snapping unit tests).

### Step 1.3 — Firestore Security Rules ✅
- Implemented [firestore.rules](../firestore.rules) covering all collections.
- Patient-side direct SDK creates enabled (decision resolved 2026-07-16).
- Staff field restrictions: status updates may only touch `status`, `paid`, `notes`, `reminderSent`, `updatedAt`.
- Verified against live emulator with rules unit tests.

### Step 1.4 — Legacy Data Migration ✅
- Created [backfill-booking-source.js](../scripts/backfill-booking-source.js).
- Backfills `bookingSource: "patient_app"` on legacy documents missing the field.
- Dry-run mode available. Verified against emulator seed data.

---

## Phase 2 — Flutter App Foundation ⏳

> Goal: Set up the Flutter Web app shell with Firebase connection, Riverpod, routing, theme, and a working login screen. No business logic yet — just the app skeleton.

### Step 2.1 — App Initialization & Firebase Connection ⏳
**Files to create/modify:**
- `lib/main.dart` — initialize Firebase, wire Riverpod `ProviderScope`, hook up go_router.
- `lib/firebase_options.dart` — generated by `flutterfire configure`, use emulator flags for dev.
- `lib/core/utils/firebase_emulator.dart` — conditionally point SDK to local emulators when `ENV=dev`.

**Acceptance criteria:**
- `flutter run -d chrome --dart-define=ENV=dev` launches without error.
- Firebase Auth and Firestore are connected to the local emulator in dev mode.

### Step 2.2 — App Theme & Design System ⏳
**Files to create:**
- `lib/core/theme/app_theme.dart` — MaterialTheme with clinic color palette, typography, spacing tokens.
- `lib/core/theme/app_colors.dart` — named color constants.

**Acceptance criteria:**
- A single `AppTheme.light()` method returns a fully configured `ThemeData`.
- No raw `Colors.X` or hardcoded hex values outside this file.

### Step 2.3 — Routing ⏳
**Files to create:**
- `lib/routing/app_router.dart` — `GoRouter` instance with all named routes.
- Route guard: redirect unauthenticated users to `/login`; redirect authenticated staff away from `/login` to `/dashboard`.

**Routes to define:**
```
/login          → LoginScreen
/dashboard      → DashboardScreen
/review-queue   → ReviewQueueScreen
/walk-in/new    → WalkInFormScreen
/appointment/:id → AppointmentDetailScreen
/services       → ServicesScreen (admin)
/settings       → ClinicSettingsScreen (admin)
/staff          → StaffManagementScreen (admin)
```

**Acceptance criteria:**
- Unauthenticated users are always redirected to `/login`.
- Authenticated staff land on `/dashboard`.
- Admin-only routes show a 403 screen if accessed by a `staff` role.

### Step 2.4 — Auth Feature ⏳
**Files to create:**
- `lib/features/auth/auth_repository.dart` — wraps `FirebaseAuth` sign-in, sign-out, and stream.
- `lib/features/auth/auth_providers.dart` — Riverpod providers for `authStateChanges`, current user.
- `lib/features/auth/login_screen.dart` — email + password login form with error handling.

**Acceptance criteria:**
- Staff can log in with credentials from `seed-emulator.js` (`staff1@clinic.test / password123`).
- Successful login navigates to `/dashboard`.
- Wrong password shows an inline error message (no snackbar pop-up).

---

## Phase 3 — Core Data Layer ⏳

> Goal: Build Riverpod providers and repository classes that stream real Firestore data. No UI yet — just the data wiring.

### Step 3.1 — Dart Models ⏳
**Files to create:**
- `lib/core/models/appointment.dart` — mirrors the TypeScript `Appointment` interface. Include `resolveBookingSource()` fallback.
- `lib/core/models/service.dart` — mirrors `Service`.
- `lib/core/models/staff_user.dart` — mirrors `StaffUser`.
- `lib/core/models/clinic_settings.dart` — mirrors `ClinicSettings`.

> ⚠️ These must be kept manually in sync with [schema-types.ts](../functions/src/schema-types.ts) until a codegen step is added. Note this in each file's header comment.

**Acceptance criteria:**
- Each model has `fromJson(Map<String, dynamic>)` and `toJson()`.
- `Appointment.fromJson` handles missing `bookingSource` gracefully (defaults to `patient_app`).

### Step 3.2 — Firestore Repositories ⏳
**Files to create:**
- `lib/features/review_queue/appointments_repository.dart` — streams pending appointments, fetches by ID, date-range query.
- `lib/features/services_admin/services_repository.dart` — streams services, CRUD operations.
- `lib/features/settings/clinic_settings_repository.dart` — reads/writes `clinicSettings/main`.
- `lib/features/staff_management/staff_repository.dart` — streams staff users, create/deactivate.

### Step 3.3 — Cloud Function Callers ⏳
**Files to create:**
- `lib/core/utils/functions_client.dart` — wrapper around `FirebaseFunctions.instance` that points to the emulator in dev mode.
- `lib/features/walk_in_booking/walk_in_functions.dart` — typed wrapper calling `createWalkInAppointment`.
- `lib/features/review_queue/status_functions.dart` — typed wrapper calling `updateAppointmentStatus` (once implemented).

---

## Phase 4 — Review Queue UI ⏳

> Goal: Build the primary staff workflow — viewing, confirming, and declining patient-submitted appointment requests.

### Step 4.1 — `updateAppointmentStatus` Cloud Function ⏳
**File to create:** `functions/src/updateAppointmentStatus.ts`

Implement per [functions-api-contract.md](./functions-api-contract.md#2-updateappointmentstatus):
- Auth guard (staff/admin only).
- Fetch appointment; throw `not-found` if missing.
- Validate transition matrix: `pending → confirmed/cancelled`, `confirmed → cancelled/completed/no-show`.
- Update only status-adjacent fields.
- Export in `index.ts`. Write Jest tests in `functions/test/updateAppointmentStatus.test.ts`.

### Step 4.2 — Review Queue Screen ⏳
**Files to create:**
- `lib/features/review_queue/review_queue_screen.dart` — list of `status == "pending"` appointments with confirm/decline actions.
- `lib/features/review_queue/appointment_card.dart` — card widget showing patient name, time, service, photo thumbnail.
- `lib/features/review_queue/review_queue_providers.dart` — Riverpod provider streaming the pending appointments.

### Step 4.3 — Appointment Detail Screen ⏳
**Files to create:**
- `lib/features/review_queue/appointment_detail_screen.dart` — full appointment details with action buttons (`Confirm`, `Cancel`, `Complete`, `No-Show`, `Mark Paid`).

---

## Phase 5 — Walk-In Booking UI ⏳

> Goal: Build the form that allows staff to book a walk-in appointment, calling `createWalkInAppointment` and showing the result.

### Step 5.1 — Walk-In Form Screen ⏳
**Files to create:**
- `lib/features/walk_in_booking/walk_in_form_screen.dart` — form with fields: first name, last name, phone, service dropdown, date+time picker, notes.
- `lib/features/walk_in_booking/walk_in_form_providers.dart` — form state management, available slots derived from `clinicSettings`.
- Time picker must show only slot-boundary times (e.g., 09:00, 09:30, 10:00) — not arbitrary minute selection.

**Acceptance criteria:**
- Successfully submitting the form calls `createWalkInAppointment`.
- On success: navigate to the appointment detail screen for the new booking.
- On `already-exists` error: show an inline message "Slot no longer available. Please pick another time."

---

## Phase 6 — Dashboard & Calendar ⏳

> Goal: Give staff a bird's-eye view of the day's and week's appointments.

### Step 6.1 — Dashboard Screen ⏳
**Files to create:**
- `lib/features/dashboard/dashboard_screen.dart` — today's appointment list + summary cards (pending count, confirmed count, walk-in count).
- `lib/features/dashboard/appointment_timeline.dart` — scrollable time-slot grid for the current day showing booked vs. free slots.
- `lib/features/dashboard/dashboard_providers.dart` — Riverpod provider for today's appointments, grouped by status.

---

## Phase 7 — Admin Tools ⏳

> Goal: Admin-only screens for managing services, clinic settings, and staff accounts.

### Step 7.1 — Services Management ⏳
**Files to create:**
- `lib/features/services_admin/services_screen.dart` — list all services, add/edit/deactivate.
- `lib/features/services_admin/service_form.dart` — form for name, duration, price, description, active toggle.

### Step 7.2 — Clinic Settings ⏳
**Files to create:**
- `lib/features/settings/clinic_settings_screen.dart` — edit working hours per day, holidays list, slot duration, reminder config.

### Step 7.3 — Staff Account Management ⏳
**Files to create:**
- `lib/features/staff_management/staff_screen.dart` — list of staff accounts, add new staff, deactivate.
- Admin cannot change their own role (guarded by Firestore rules).

---

## Phase 8 — Notifications ⏳

> Goal: Implement appointment reminder and status-change notifications. Ownership confirmed as this app's responsibility (decisions-log.md #5).

### Step 8.1 — `sendReminders` Scheduled Function ⏳
**File to create:** `functions/src/sendReminders.ts`

- Runs on a Cloud Scheduler cron (e.g., every 30 minutes).
- Queries `appointments` where `status == "confirmed"`, `reminderSent == false`, and `appointmentDateTime` is within `clinicSettings.reminderHoursBefore` of now.
- Sends email/SMS via Resend or Twilio for appointments with `userEmail` present.
- Sets `reminderSent = true` after a successful send.
- Skips walk-ins with `userEmail == null`.

### Step 8.2 — `onAppointmentStatusChange` Trigger ⏳
**File to create:** `functions/src/onAppointmentStatusChange.ts`

- Triggered on Firestore `onUpdate` of `appointments/{id}`.
- On `confirmed`: send booking confirmation to patient.
- On `cancelled`: send cancellation notice to patient.
- Only fires for `userId != null` (patient-app bookings) — walk-ins have no notification channel.

---

## Phase 9 — Polish & Deployment ⏳

### Step 9.1 — Responsive Layout Pass ⏳
- Ensure all screens are usable on desktop browser widths (min 1024px).
- Sidebar navigation for wide screens; bottom nav for narrow.

### Step 9.2 — Error Handling & Loading States ⏳
- All async Riverpod providers must handle `loading`, `data`, and `error` states explicitly.
- No unhandled exceptions reaching the UI.

### Step 9.3 — Production Firestore Rules Review ⏳
- Audit [firestore.rules](../firestore.rules) against the patient app team's confirmed write patterns.
- Remove any remaining `⚠️ ASSUMPTION` comments once confirmed.

### Step 9.4 — Run Backfill Migration on Production ⏳
```bash
# Production run (no emulator env var — targets real Firestore)
GOOGLE_APPLICATION_CREDENTIALS=path/to/key.json \
GCLOUD_PROJECT=oralscope-78cda \
node scripts/backfill-booking-source.js
```

### Step 9.5 — Firebase Hosting Deploy ⏳
```bash
flutter build web --dart-define=ENV=prod
firebase deploy --only hosting,functions,firestore:rules
```

---

## Deferred / Out of Scope

| Item | Reason |
|---|---|
| `analyzeWalkInImage` | Image analysis not needed for walk-ins (decision #6 resolved). Deferred indefinitely. |
| Patient-facing UI | Fully owned by the separate patient app — not built here. |
| Online payment integration | Out of scope. `paid` flag is managed manually by staff. |
| iOS/Android builds | Staff app is web-only. |

---

## Dev Workflow Rules

1. **Emulator-first**: All development and testing runs against the local Firebase emulator. Never test against production Firestore.
2. **Backend before UI**: Implement and test each Cloud Function before building the UI that depends on it.
3. **Test every function**: Every Cloud Function must have a Jest test file in `functions/test/`. No exceptions.
4. **Schema parity**: Any change to [schema-types.ts](../functions/src/schema-types.ts) must be mirrored manually in the corresponding Dart model in `lib/core/models/`.
5. **Rules with every feature**: When a new write pattern is introduced (new collection, new field update), update `firestore.rules` and add a rules test in `firestore.rules.test.ts` in the same step.
6. **Commit discipline**: Each completed step above should map to one logical git commit. Use the step number as the commit prefix, e.g. `feat(2.4): implement staff login screen`.
